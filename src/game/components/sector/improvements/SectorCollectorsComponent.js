// marks the sector as containing collectors; actual data is in SectorImprovementsComponent
define(['ash'], function (Ash) {
	var SectorCollectorsComponent = Ash.Class.extend({
		
		constructor: function () {
		},
		
	});

	return SectorCollectorsComponent;
});
