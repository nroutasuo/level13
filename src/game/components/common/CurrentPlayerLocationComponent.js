// Defines the given entity (level/sector) as the player's current location
define(['ash'], function (Ash) {
	var CurrentPlayerLocationComponent = Ash.Class.extend({
		constructor: function () {
		}
	});

	return CurrentPlayerLocationComponent;
});
